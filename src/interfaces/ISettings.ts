export interface ISettings {
    fontSize: number;
    selectedTheme: string;
    selectedSemester: number;
    selectedEndpoint: string;
    selectedLanguage: string;
    scheduleGroupName: string;
    isShowFullDisciplineName: boolean;
}